# ---------------------------------------------------------
# 适配当前环境（sam3, Python 3.12, Torch 2.7.0+cu126）
# 原则：
# 1) 不覆盖当前已安装的 torch / torchvision / torchaudio
# 2) 尽量不强制降级当前环境中已可用的大型依赖
# 3) 保留 FoundationPose 常见核心依赖
# 4) 注释掉高风险 / 非核心 / 易冲突依赖
# ---------------------------------------------------------

# ---------------------------------------------------------
# PyTorch 相关依赖：保持注释，防止覆盖当前 CUDA 12.6 环境
# 当前环境已安装：
# torch==2.7.0+cu126
# torchvision==0.22.0+cu126
# torchaudio==2.7.0+cu126
# ---------------------------------------------------------
# --extra-index-url https://download.pytorch.org/whl/cu118
# torch==2.0.0+cu118
# torchvision==0.15.1+cu118
# torchaudio==2.0.1+cu118

# ---------------------------------------------------------
# 基础开发工具
# ---------------------------------------------------------
jupyterlab==4.1.5
ipywidgets==8.1.2

# ---------------------------------------------------------
# Common Libraries
# 说明：
# - numpy 保持 1.x，兼容性更稳
# - scipy / scikit-learn / scikit-image / numba 当前环境已有更高版本，
#   这里不强制覆盖，避免把现有环境降级
# ---------------------------------------------------------
numpy==1.26.4
# scipy==1.12.0
# scikit-learn==1.4.1.post1
# scikit-image==0.22.0
pyyaml==6.0.1
ruamel.yaml==0.18.6
ninja==1.11.1.1
h5py==3.10.0
# numba==0.59.1
pybind11==2.12.0

# ---------------------------------------------------------
# CV / 3D Libraries
# 当前环境已有更高版本的 opencv / open3d，避免强制降级
# ---------------------------------------------------------
imageio==2.34.0
# opencv-python==4.9.0.80
# opencv-contrib-python==4.9.0.80
plotly==5.20.0
# open3d==0.18.0
pyglet==1.5.28
pysdf==0.1.9

# ---------------------------------------------------------
# Geometry / Mesh
# ---------------------------------------------------------
trimesh==4.2.2
xatlas==0.0.9
rtree==1.2.0

# ---------------------------------------------------------
# PyRender
# ---------------------------------------------------------
pyrender==0.1.45
pyOpenGL>=3.1.0
pyOpenGL_accelerate>=3.1.0

# ---------------------------------------------------------
# 其他依赖
# 说明：
# - 保留更可能与 FoundationPose 主流程有关的
# - 去掉/注释掉明显非核心、容易引发额外依赖问题的包
# ---------------------------------------------------------
meshcat==0.3.2
webdataset==0.2.86
omegaconf==2.3.0
pypng==0.20220715.0
Panda3D==1.10.14
simplejson==3.19.2
bokeh==3.4.0
roma==1.4.4
seaborn==0.13.2
pin==2.7.0
openpyxl==3.1.2
torchnet==0.0.4
wandb==0.16.5
colorama==0.4.6
GPUtil==1.4.0
imgaug==0.4.0
xlsxwriter==3.2.0

# 当前环境已有更高版本，避免降级
# timm==0.9.16
albumentations==1.4.2

# 非核心 / 高风险 / 容易引入额外冲突，先注释
# nodejs==0.1.1
# objaverse==0.1.7
# g4f==0.2.7.1
# ultralytics==8.0.120

pycocotools==2.0.7
py-spy==0.3.14
pybullet==3.2.6

# 非核心且可能带系统层依赖问题，先注释
# videoio==0.2.8

kornia==0.7.2

# 当前环境已有更高版本，避免降级
# einops==0.7.0

transformations==2024.6.1
joblib==1.3.2
warp-lang==1.0.2

# ---------------------------------------------------------
# For PyTorch3D
# 注意：这里只保留 Python 侧常见依赖
# PyTorch3D 本体不要在 requirements.txt 里装
# 因为你当前环境是 Python 3.12 + Torch 2.7.0 + CU126，
# 原 wheel 不兼容，后续如需要请单独源码编译
# ---------------------------------------------------------
fvcore==0.1.5.post20221221
